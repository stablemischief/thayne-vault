# Memory — March 24, 2026

## AgFin — V4 Synthetic Data Created

- V4 Synthetic Data directory created at `jw-planning/Working/Synthetic-Data/V4 Synthetic Data/`
- All 3 applicants updated: james_thornton, delta_ag_partners, hernandez_family_farms
- Key changes from V3 → V4:
  - Crop Year: 2024 → 2026
  - Prior Year: 2023 → 2025
  - Schedule F: 2023 → 2025 (using actual IRS 2025 Schedule F PDF form)
  - Crop insurance applications stripped of APH detail (now matches NAU Country blank form style)
  - APH detail stays in Prior Year Schedule of Insurance docs only

## Schedule F PDF Form Filling (done by Thane directly, no Archon)

- Used pymupdf + Ghostscript to fill IRS 2025 Schedule F fillable PDF forms
- Ghostscript installed via brew for proper PDF flattening
- Key lesson: f1_47–f1_58 are the 32a–32f (description + amount) fields; f1_59 = line 33 total expenses; f1_60 = line 34 net profit
- Final filled PDFs saved as `_filled.pdf` variants (Ghostscript-flattened) in each applicant's PDF/ directory
- Blank 2025 Schedule F reference form saved at: `V4 Synthetic Data/reference-forms/schedule-f-2025-blank.pdf`

## UI/UX Review — Module Review Phase

James shared screenshots of the current module review UI. Key findings:
- Form view works fine for flat, non-repeating sections (Identity, Financial Statement)
- Breaks badly for repeating entity sections (APH crops, FSA farms, Equipment)
- Split pane PDF viewer already exists in the app
- Recommended approach:
  - Keep form view for non-repeating sections
  - Entity Cards (collapsed summary + expand) for crop/farm sections
  - Inline editable data grid for equipment list
  - `display_mode` property on section config: `"form"` | `"entity-cards"` | `"data-grid"`
- No mockup built yet — James hasn't asked for one explicitly

## Per-County/Per-Farm Field Namespacing — Mitch's Plan Review

Mitch wrote a plan to fix silent data loss when multiple docs of same type are uploaded (e.g., 2 APH docs for 2 counties). Reviewed and approved with 4 callouts. Review doc written to:
- `jw-planning/Working/implementation-plans/mitch-review-per-county-namespacing.md`

**Plan summary:** Change `DISTINCT ON (field_id, owner_type)` → `DISTINCT ON (field_id, owner_type, owner_index)`. Use `(owner_type, owner_index)` tuple key in field resolution layer instead of renaming 500+ field IDs.

**4 callouts:**
1. `owner_counts` source: use document upload slots (all statuses), not just extracted docs
2. Sequence against extraction-phase-fix: Phase 3 + `populate_module_fields()` must be designed together
3. Spouse DL tuple key: `("applicant", 0)` and `("spouse", 0)` are distinct — document explicitly; coordinate with `update_application_name_from_dl()` fix
4. PATCH endpoint: make `owner_type`/`owner_index` required (not optional) for per-county/per-farm fields at Pydantic schema level

**Approved sequencing:**
1. Phase 1 first (fix data loss, no API changes) — ship immediately
2. extraction-phase-fix Phase A/B in parallel
3. Phase 3 + populate_module_fields() designed together — hold until coordinated

## Thornton Applicant JSON → MD Conversion

James sent `james_thornton_applicant_data.json` — converted to readable markdown and sent via Discord (Slack cross-platform not available — no shared session context between Discord and Slack instances of Thane).

## Process Note — Writing Files to Repo

Confirmed rule (from James Mar 16): Writing planning/design docs directly to `jw-planning/` is fine — no need to go through Archon for docs. Archon is for code files only.

## Slack ↔ Discord Isolation

Confirmed Mar 19: Thane instances on Discord and Slack have no shared session context. James can interact with Thane on both platforms but they don't share conversation memory. Each session is isolated.
