# 2026-03-29 — Daily Notes

## Nesvick Podcast — Active Work

### Current Branch
`task-feat-shareable-episode-url` — all active development happening here
Coolify must be redeployed to pick up latest code changes before testing

### Latest Commits on Branch (as of this morning)
- `833209e` — balance instruction added to outline template
- `a074a9a` — Phase 2 transcript template override (exact turns + 40-60 words/turn)
- `759253f` — share player UI polish (briefing truncate, talking points, player cutoff)
- `45b15ec` — share page SSR fetch fix (localhost:5055)
- `cdda1bf` — ClerkTokenProvider + remove auth from share endpoint

### TTS Normalizer (NOT YET MERGED)
Branch `feat/tts-text-normalizer` — Archon built it with colon handling added
Validation report: `.agents/research/tts-normalizer-merge-validation.md` ✅
Still needs to be copied to `task-feat-shareable-episode-url` and pushed

### VPS Episode Profile State
- num_segments: **5** (updated Sat Mar 28, live immediately)
- Briefing includes: no em dashes, no colons mid-sentence, no special chars
- Colon rule added to briefing (live, no redeploy needed)

### Transcript Balance Issue (UNRESOLVED)
Problem: With num_segments=5, the outline still front-loads weather/planting
and Treasury is completely missing from the latest transcript (Mar 29 generation).
Latest transcript: 864 words, 18 turns, but turns 16-18 are beef+wrap with
Treasury never appearing at all.

Key question outstanding: Did James redeploy before generating the Mar 29 transcript?
The balance instruction code change (833209e) requires a redeploy to take effect.
If no redeploy happened, the old outline template ran without the balance instruction.

### What's Needed Before Merging PR #22 to Main
1. Redeploy Coolify to pick up 833209e (balance instruction + TTS normalizer)
2. Confirm TTS normalizer has been copied to feature branch and pushed
3. Generate a test transcript — verify 5 balanced segments (grains, livestock, treasury)
4. Generate audio — confirm no em dash, colon, or foreign language artifacts
5. Generate shareable URL — confirm player page loads with talking points
6. Merge PR #22 to main
7. Switch Coolify back to main
8. Update seed/update-tts-config.sh with current VPS values

### Backport to Stable-Notebook (PENDING, after Nesvick stable)
- A.13: basicModeEpisode cache invalidation fix
- A.14: TTS normalizer
- A.11: Shareable URL feature (opt-in, requires Supabase setup)

## Process Lessons Reinforced This Week
- Never make direct code changes to repo — always Archon
- Always get Archon build plan before build
- Briefing-only constraints are unreliable — code-level template overrides are required
- Phase 1 (briefing) failed because the growing context triggered empty JSON responses on segment 4
- Phase 2 (template override) is the right architectural approach for transcript constraints
